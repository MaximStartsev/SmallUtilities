<UserControl x:Class="MaximStartsev.SmallUtilities.Napominalka.Views.TasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MaximStartsev.SmallUtilities.Napominalka.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <DataGrid ItemsSource="{Binding Tasks}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Логин" Width="200" Binding="{Binding Login}"/>
                <DataGridTextColumn Header="Дата регистрации" Width="150" Binding="{Binding RegistrationDate}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
